{#
/**
 * @file model_cv.html.twig
 * Default theme implementation to present Model cv data.
 *
 * This template is used when viewing Model cv pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_model_cv()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('model_cv') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
