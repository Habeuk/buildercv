{% if item %}
  <div class="infos field-svg d-flex">
    {% if item.value %}
      <div class="svg">
        {{ item.text|raw }}
      </div>
    {% endif %}
    {% if item.text %}
      <div class="text font-weight-bold">
        {{ item.value }}
      </div>
    {% endif %}
  </div>
{% endif %}
